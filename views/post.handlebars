<div class="card text-center">
  <div class="card-body">
    <h2 class="card-title">{{ title }}</h2>
    <h5 class="card-subtitle mb-2 text-muted">Created by {{user.username}} on {{format_date date_created}}</h5>
    <p class="card-text">{{content}}</p>
  </div>
</div>